import 'package:flutter/material.dart';

class TabletMobileMainComponent extends StatelessWidget {
  const TabletMobileMainComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
